<xi-button
  bind:click="click"></xi-button>
<xi-steps
  current="{{ currentIndex }}">
  <xi-step
    wx:for="{{ options }}"
    wx:key="{{ index }}">
    <view slot="title">
      步骤{{ index }}
    </view>  
  </xi-step>
</xi-steps>